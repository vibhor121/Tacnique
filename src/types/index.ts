export type User = {
  id: number;
  name: string;
  username: string;
  email: string;
  department: string;
};

export type UserForm = {
  firstName: string;
  lastName: string;
  email: string;
  department: string;
};
